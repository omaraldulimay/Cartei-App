<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/text_progress"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Lernfortschritt"
        android:textSize="18sp"
        android:layout_marginBottom="16dp" />

    <Button
        android:id="@+id/button_karten_loeschen"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Alle Karten löschen"
        android:layout_marginBottom="8dp" />

    <Button
        android:id="@+id/button_reset_gelernt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Gelernt-Status zurücksetzen"
        android:layout_marginBottom="16dp" />

    <!-- 🆕 RecyclerView für die Kartenanzeige -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewKarten"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingTop="8dp" />
</LinearLayout>
